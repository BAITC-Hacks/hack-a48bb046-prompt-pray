"""
Общий код для всех микросервисов: настройки, логирование, БД, JWT, middleware.

Каждый модуль подключается явно (`from common.db import Database`), поэтому
сервис без БД (например, api_gateway) не тянет за собой SQLAlchemy.
"""
