# Отчёт Улана по backend-задачам AI Sana

**Автор:** Улан  
**Команда:** team-2  
**Дата:** 23 сентября 2026 года

В этом документе я описываю свой вклад в backend проекта: роли пользователей
в `auth_service` и каркас `catalog_service`. Также фиксирую выполненные проверки
и статус связанных задач. Отчёт относится к изменениям T-1 и T-2; более поздняя
реализация доменной логики в ветке другого участника описана отдельно.

## T-1 — роли пользователей

Добавил разделение пользователей на две роли:

| Значение | Назначение |
|---|---|
| `business` | Представитель бизнеса, который будет создавать и подтверждать задачи, выбирать команды |
| `student` | Студент, который будет просматривать каталог и отправлять предложения |

Роль сохраняется в модели `User`, принимается при регистрации, возвращается
в `GET /api/v1/users/me` и включается в access-токен JWT. Общий обработчик
`common.auth` передаёт роль в `TokenUser`, чтобы другие сервисы могли проверять права.
Сами проверки прав на действия с задачами относятся к следующим доменным задачам.

Для совместимости с прежними клиентами регистрация без поля `role` использует
`business`. Неизвестное значение, например `admin`, отклоняется с кодом `422`.
Поле `is_admin` остаётся отдельным признаком и не заменяется ролью.

Пример запроса `POST /api/v1/auth/register`:

```json
{
  "email": "student-check@example.com",
  "username": "student_check",
  "password": "Check-pass-123",
  "role": "student"
}
```

После входа access-токен содержит `"role": "student"`, а `/users/me` возвращает
это поле вместе с остальными данными пользователя. Пароль и его хеш в ответ не входят.

Изменённые файлы:

- `backend/auth_service/app/models/user.py` — хранение роли.
- `backend/auth_service/app/schemas/user.py` — контракт регистрации и ответа.
- `backend/auth_service/app/services/auth.py` — сохранение роли и выдача JWT.
- `backend/common/auth.py` — общий тип роли и чтение claim из токена.
- `backend/auth_service/tests/test_auth_flow.py` — проверки обеих ролей и неверного значения.

**Результат проверки:** 8 тестов `auth_service` прошли.

В моей первоначальной реализации T-1 отдельной миграции существующей таблицы
`users` не было. `create_all` создаёт новую таблицу, но не добавляет колонку в уже
существующую. Для старой базы нужна миграция; она появилась позднее в ветке
`feat/ai-content-service` другого участника.

## T-2 — каркас сервиса каталога

На основе структуры `example_service` создал `backend/catalog_service`:

```text
catalog_service/
├── app/
│   ├── api/            маршруты и зависимости авторизации
│   ├── core/           настройки сервиса
│   ├── db/             подключение к своей базе данных
│   ├── models/         место для доменных моделей
│   ├── repositories/   место для запросов к базе
│   ├── schemas/        схемы запросов и ответов
│   ├── services/       место для бизнес-логики
│   └── main.py         приложение и его запуск
├── tests/
├── Dockerfile
├── pytest.ini
└── requirements.txt
```

Подключил сервис к gateway через `CATALOG_SERVICE_URL` и префикс `catalog`.
Добавил его в `run_local.py`, `run_tests.py`, зависимости локального запуска,
оба Compose-файла, `.env.example` и скрипт инициализации PostgreSQL.
Обновил архитектурную документацию.

При локальном запуске каталог использует порт `8004` и SQLite-файл
`backend/data/catalog_service.db`. Для PostgreSQL предусмотрены отдельные
`CATALOG_DB_NAME`, `CATALOG_DB_USER` и `CATALOG_DB_PASSWORD`.

| Маршрут | Назначение |
|---|---|
| `GET http://localhost:8004/health` | Проверяет готовность каталога и доступность его БД |
| `GET http://localhost:8004/healthz` | Проверяет, что процесс сервиса работает |
| `GET http://localhost:8000/api/v1/catalog/status` | Проверяет проход через gateway; нужен access-токен |
| `GET http://localhost:8000/health` | Показывает состояние всех подключённых сервисов |

Маршрут `/catalog/status` возвращает `{"status":"ready"}`. В рамках T-2 модели
бизнес-задач, публикация, рейтинг и отклики не реализовывались.

Дополнительно исправил два препятствия для запуска:

- В Compose добавлена отсутствовавшая запись `ai_service`, который gateway уже
  учитывал в общей проверке здоровья.
- В `start.bat` добавлен запасной запуск через Python из `.venv`, если `py` и
  `python` отсутствуют в `PATH`.

В `run_tests.py` добавлен явный пропуск сервисов, у которых ещё нет файлов тестов.
На момент проверки это относилось к `ai_service`; пропуск не означает, что его
поведение было проверено тестами.

## Как повторить проверки

Из корня репозитория в PowerShell:

```powershell
cd backend
.\start.bat --no-reload
```

В отдельном окне PowerShell:

```powershell
curl.exe http://localhost:8004/health
curl.exe http://localhost:8000/health
```

Для проверки маршрутизации получи access-токен через вход в
[Swagger auth_service](http://localhost:8001/api/v1/docs), затем выполни:

```powershell
$accessToken = Read-Host 'Access token'
curl.exe -H "Authorization: Bearer $accessToken" http://localhost:8000/api/v1/catalog/status
```

Для запуска тестов из папки `backend`:

```powershell
.\.venv\Scripts\python.exe scripts/run_tests.py
```

Зафиксированный результат после T-2:

| Сервис | Результат |
|---|---|
| `api_gateway` | 10 passed |
| `auth_service` | 8 passed |
| `example_service` | 4 passed |
| `catalog_service` | 1 passed |
| `ai_service` | Пропущен: на тот момент тестов не было |

Запуск через `start.bat`, прямой `/health` каталога, общий `/health` и защищённый
маршрут через gateway проверены: получены ответы `200`. Порт `8111` не использовался.
Compose-файлы проверены на синтаксис YAML. Запуск Docker-контейнеров не проверялся:
Docker был недоступен в окружении проверки.

## Статус связанных задач

| Задача | Мой вклад и состояние |
|---|---|
| T-1 | Реализованы роли, JWT claim и ответ `/users/me`; тесты прошли |
| T-2 | Реализован каркас каталога и его подключения; локальный запуск проверен |
| T-4 | Проведена проверка зависимостей; собственная реализация в этой ветке не добавлялась |
| T-6 | Выполнено ревью реализации в ветке другого участника: сортировка, пагинация и отсутствие порога рейтинга предусмотрены |
| T-8 | Выполнено ревью обработки ошибок: используются `ai_*`, сырые ошибки провайдера скрыты; общие `not_found`, `unauthorized` и стандартный `422` разрешены API-стандартом |

Реализация каталога, отчёт по T-8 и дополнительные тесты находятся в ветке
`feat/ai-content-service` другого участника. Эти изменения я не включаю в список
собственных реализаций. Тесты этой ветки в рамках моего ревью не запускались.
Окончательный статус задач на доске зависит от командного ревью и слияния веток.

## Связанные изменения и документы

- T-1: коммит `18c37fd` — роли пользователей.
- T-2: коммит `82fa872` — каркас каталога, ветка `feat/T-2-catalog-service`.
- [Архитектура](architecture.md).
- [Стандарт API](api-standards.md).
- [Инструкция запуска backend](../backend/README.md).
